# MoLBWA 인수인계 — 2026-09-23

시선 제어 로봇팔(SO-101). 안경으로 컵 3개 중 하나를 응시하면 팔이 집는다.

---

## 결론부터: 다음 세션이 할 일

**SLAM을 버리고 AprilTag 직결로 간다.** 이유는 아래 §3.

```
눈 → 시선 픽셀 (u,v)
     ↓ 역투영 (ocams_calib.RECTIFIED_K)
AprilTag → T_hc_ab (매 프레임, 절대 스케일 포함)
     ↓ 광선을 팔 베이스 좌표계로
테이블 평면(z=-0.11) 교차 → 컵 위치
```

SLAM 없음, **스테레오 깊이 없음**. 오늘 막힌 두 가지가 다 빠진다.

작업 순서:
1. **시선 캘리브레이션** (진행하려다 중단됨 — 아래 §5)
2. `origin/fix/so101-runnable`의 `arm/`을 현재 브랜치로 가져오기
3. 태그 인쇄·부착·실측
4. 글루 코드 (아래 §4에 정확히 무엇인지 적어둠)

---

## 1. 오늘 고친 것 (커밋 `1b59a39`)

### 융합 코드 — `gaze_on_scene.py`에 빠져 있던 마지막 연결부

시선 픽셀은 계산하는데 **밖으로 내보내는 코드가 한 줄도 없었다.** 로봇팔 쪽이
영영 아무것도 못 받는 구조였다. UDP 배관(`gaze_udp_sender.py` +
`gaze_bridge_node.py`, 포트 55055 JSON)은 이미 있었으므로 호출만 붙였다.

- `fusion.quat_xyzw_to_matrix()` / `pose_to_matrix()` — `/orbslam3/pose` → `T_WS`
- `RosFrameSource.enable_pose()` / `latest_pose()` — 기존 rclpy 노드에 구독을 얹음
- `fuse_and_send()` — 시선픽셀 + 깊이 + `T_WS` → `p_W` → UDP
- `test_fuse_and_send.py` — 10개 검증 전부 PASS

설계에서 신경 쓴 것:
- **무효일 때 침묵하지 않고 `valid=False`를 보낸다.** 안 보내면 `dwell_detector`가
  마지막 유효점을 붙들고 팔이 그리로 간다. 종료 시에도 한 발 보낸다.
- `--scene-flip`이면 `(u,v)`를 되돌려서 깊이 조회/역투영에 쓴다. 표시용 이미지만
  뒤집히는데 `K`와 disparity는 뒤집기 전 좌표계다. 안 되돌리면 **17.6cm 어긋난다**
  (테스트 8번이 대조군으로 이걸 증명한다).

### ORB-SLAM3 노드 버그 — `stereo-inertial-node.cpp`

⚠️ **이 저장소가 아니다.** `ros2_ws/src/orbslam3_ros2/`는 별도 클론(zang09/ORB_SLAM3_ROS2,
detached HEAD)이고 **커밋하지 않았다.** 작업 트리에 그대로 있다. 그 디렉토리에서
`git reset --hard` / `git clean`을 돌리면 사라진다.

고친 내용 — `GrabImageLeft/Right`가 버퍼를 1장만 유지하고 있었다:

```cpp
if (!imgLeftBuf_.empty()) imgLeftBuf_.pop();   // 항상 최신 1장만
imgLeftBuf_.push(msgLeft);
```

짝 맞추기 루프는 `size() > 1`일 때만 pop한다. 좌우가 한 프레임만 엇갈려도
(33ms > 임계 10ms) 영원히 해소 못 하고 `continue`로 제자리를 돈다.

| | 이전 | 이후 |
|---|---|---|
| `big time difference` | 7798회 | **0** |
| `/orbslam3/pose` | 3.97Hz | **30.3Hz** |
| 로그 총량 | 8814줄 | 119줄 |

버퍼 10장으로 늘리고, `continue` 경로에 sleep이 없어 코어를 태우던 것도 고쳤다.

### 토픽 충돌 — `~/slam.sh`

`/camera/left`에 발행자가 **2개**였다(Pi의 원본 + 데스크탑 압축해제 노드). 두 경로의
지연이 달라 프레임 순서가 뒤섞였다. 압축 해제를 `/slam/{left,right}`로 분리하고
리매핑으로 붙였다. `Frame with a timestamp older than previous frame` 소멸.

### `~/slam.sh` 자동 재시작

추적을 잃으면 IMU만으로 적분하다 회전이 NaN이 되고 Sophus가 프로세스를 죽인다
(`SO3::exp failed! omega: 1.3e+22`, exit 250). 3초 뒤 자동 재시작하게 했다.

### AprilTag 시트 — `tools/make_apriltag_sheet.py`

60mm tag36h11 4장(id 0~3) PDF. 생성 후 `pupil_apriltags`로 재검출해 물리 치수를
검증했다: **59.99~60.01mm**.

⚠️ **통째로 붙이면 안 된다.** 평면 번들은 재투영 오차 **0.000px인데 팔 베이스가
120mm 틀린** 거울상 해를 만든다(`notes/2026-08-19_so101_calib_anchor_fixes.md`).
재투영 게이트로는 절대 못 거른다. 4장을 잘라 **두 단(깊이차 30mm 이상)**으로 붙일 것.
템플릿: `calibration/apriltag_36h11_60mm_bundle_template.yaml`

---

## 2. 오늘 알아낸 것 — 진단 결과

### ★ SLAM 추적 실패의 진짜 원인은 영상 품질이었다

하루 종일 SLAM 파라미터와 움직임을 탓했는데, 실측해보니 **들어가는 영상 자체가
나빴다**. `/slam/left` 40프레임 측정:

| 지표 | 측정값 | 정상 |
|---|---|---|
| ORB 특징점 | 485개 | 목표 1200 |
| **선명도**(라플라시안 분산) | **20.1** | 100+ 선명, 50 미만 흐림 |
| **과노출 픽셀** | **24%** | 5% 미만 |
| 빈 화면구역 | 16칸 중 5칸 | 0~1칸 |

**결정적 단서: 40프레임 내내 선명도가 20.0~20.2로 거의 안 변한다.** 모션 블러라면
움직임에 따라 출렁여야 한다. 안 출렁인다 = **고정된 원인**:

1. 렌즈 초점이 안 맞았을 가능성 (oCamS 수동 초점?)
2. Pi의 JPEG 압축 품질이 낮을 가능성 (고주파 손실)
3. 자동 노출이 형광등에 속고 있음 (24% 날아감)

셋 다 확인·수정 가능하다. **SLAM을 다시 쓸 일이 생기면 여기부터 봐야 한다.**
특징점이 485 → 1000대로 오르면 추적 상실 523회는 대부분 사라질 것이다.

진단 스크립트는 `/tmp` 스크래치에 있었으므로 없다. 다시 쓰려면: `/slam/left`나
`/camera/left` 구독 → `cv2.ORB_create(1200).detect()` 개수,
`cv2.Laplacian(img, CV_64F).var()`, `(img>=250).mean()`.

### 배제된 가설들 (다시 의심하지 말 것)

- **IMU-카메라 시간 오프셋** — Kalibr 값 2.58ms / 7.89ms. 30fps 프레임 간격 33ms의
  10% 미만이라 523회 상실의 원인이 못 된다.
- **`IMU.T_b_c1`이 미검증 identity** — 틀렸다. 실측 Kalibr 값이 들어 있다
  (`oCamS.yaml:72`, 2026-08-07).
- **USB 대역폭** — 아니었다. 원인은 **연장 케이블**이었고, 직결 후 oCamS가 처음으로
  SuperSpeed(5000Mbps)로 잡혔다.

### 전원

보조배터리(5V/3A, 15W)로 전부 정상 동작한다. Pi 5가 `usb_max_current_enable=0`으로
USB를 600mA로 제한하지만 실제로 문제없었다:
토픽 30/30/100/29.6Hz, 전압 5.05V, 스로틀링 0, 커널 이상 로그 0.
`config.txt` 손댈 필요 없음.

---

## 3. 왜 SLAM을 버리는가

사용자 질문("SLAM은 씬 카메라만 쓰는데 눈동자 추적이 의미 없는 거 아니냐")에서
나온 결론이다.

둘은 다른 질문에 답한다 — SLAM은 "머리가 방 어디에 있나", 눈은 "그 시야 안에서
어디를 보나". 컵 3개가 놓여 있고 고개는 정면일 때 **어느 컵인지는 눈만 안다.**

그런데 **로봇팔 좌표로 가려면 어차피 AprilTag 등록이 필요하다.** 태그가 씬 카메라에
보이는 상태라면 태그에서 곧바로 카메라↔팔 관계가 나오므로 SLAM을 건너뛸 수 있다.
씬 카메라 화각이 약 69°라, 컵 근처(=팔 베이스)에 태그가 있으면 컵을 볼 때 같이 들어온다.

SLAM이 실제로 필요한 경우는 **태그가 안 보일 때**(사용자가 로봇에서 등을 돌림)뿐이다.
시연 시나리오가 "책상 앞에서 컵을 본다"로 한정되면 필요 없다.

**추가 이득:** `docs/12`가 스테레오 깊이를 "로봇팔 제어에 사용 금지"로 못박아 놨다
(1.5m에서 12% 오차). `arm/perception.py`에 **깊이를 안 쓰는** 방법이 이미 있다 —
시선 광선 ∩ 알려진 테이블 평면(`config.yaml: task.table_z = -0.11`). 그러면
아래 §6의 baseline 1.63배 충돌이 통째로 무의미해진다.

---

## 4. 재사용할 것 / 새로 쓸 것

조사 결과 **필요한 것의 90%가 이미 `origin/fix/so101-runnable`에 있다.**

### 재사용 (재작성 금지 — 전부 실패를 겪고 추가된 방어장치가 들어 있다)

| 무엇 | 어디 |
|---|---|
| `TagBundleDetector.detect(gray) -> T_hc_ab` ★핵심, SLAM 의존성 없음 | `arm/anchor.py:198-282` |
| `build_bundle_obj_pts()` + `tag_axes` 규약 | `arm/anchor.py:155-183` |
| `backproject(u,v,depth,K)` | `arm/perception.py:34-37` |
| `ray_plane_intersect` / `cup_position_on_table` ★깊이 없이 컵 위치 | `arm/perception.py:53-91` |
| `robust_depth()` | `arm/perception.py:94-107` |
| `anchor:` config 블록 | `arm/config.yaml:47-82` |
| `depth_at()`, `make_stereo_matcher()` | `src/gaze_on_scene.py` |
| `RECTIFIED_K` (절대 하드코딩 금지) | `src/ocams_calib.py` |

`TagBundleDetector.detect()` 안에 이미 들어있는 것: 여러 태그 코너를 **한 번에** PnP
(단일 태그 회전 뒤집힘 방지), **cheirality 필터**(태그가 카메라 뒤에 있는 해 제거 —
재투영 0px로 통과했었다), **모호성 필터**(후보해가 2cm 넘게 벌어지면 관측 폐기),
재투영 3px 게이트.

### 건너뛸 것

`AnchorTracker` 전체 — SLAM latch/drift/Atlas map_id/상태머신. 존재 이유가
"태그가 안 보일 때 SLAM으로 메우기"뿐이다. 태그 직결에서는 `detect()` 결과를
매 프레임 그대로 쓰면 된다:

```python
p_ab = inv(T_hc_ab) @ p_hc     # 이게 등록의 전부다
```

⚠️ **이름 함정:** `T_headcam_to_armbase()`는 `T_hc_ab`를 반환하는데 이건
armbase→headcam 변환이다(`p_hc = T @ p_ab`). 메서드 이름이 거꾸로 읽힌다.
docstring이 정답이다.

### 새로 써야 할 것

1. 글루: `TagBundleDetector.detect(좌영상)` → `T_hc_ab`, 시선 픽셀 역투영 →
   광선 → 팔 좌표계 → 테이블 평면 교차
2. 유효성 정책 (`AnchorState`/`can_execute_grasp` 대체) — 오래되거나 거부된
   태그 관측에서 팔을 움직이면 안 된다
3. `head_cam → base_link` 동적 TF 노드 (`calib_tf_publisher` 자리에 끼우면
   `target_resolver`/`arm_server`가 그대로 동작) **또는** `arm/run_demo.py`식 직접 경로
4. 이상적으로는 **그리퍼 태그 핸드아이** — 아래 §6의 "자로 재기"를 없앤다

### 브랜치 병합 시 주의

`arm/test_pipeline.py`가 **옛 intrinsics**(`fx=484.711`, `cx=326.836`,
`baseline 10.67cm`)를 하드코딩 assert한다. 현재 값은 `fx=461.237`, `cx=370.495`.
가져오면 테스트가 깨지니 상수를 갱신할 것.

---

## 5. 중단된 지점 — 시선 캘리브레이션

사용자가 "캘리브레이션 먼저 하겠다"고 해서 카메라를 올리고 뷰어를 띄우려던 중
계정 전환으로 중단됐다.

**상태:** Pi 카메라 스택 **가동 중**(토픽 29.6/30.2/30.2Hz 확인). 데스크탑 뷰어는
띄우다 말았다(`gaze_on_scene.py`가 exit 1로 죽음 — 원인 미확인, 로그가 비어 있었다.
`-u` 없이 띄우면 stdout 버퍼링 때문에 아무것도 안 보인다. **항상 `python3 -u`로 띄울 것**).

재개 명령:
```bash
ssh a-hri@192.168.50.189 '~/ocams.sh'          # 이미 떠 있으면 생략
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
unset PYTHONPATH CONDA_PREFIX CONDA_DEFAULT_ENV
export DISPLAY=:1
source /opt/ros/jazzy/setup.bash
cd ~/MoLBWA-gaze-hri/src
python3 -u gaze_on_scene.py --no-rerun --source ros
```

**기존 캘리브 파일** (새로 하면 덮어씀):
- `gaze_scene_affine.json` — 10점, 평균오차 **11.52px** (9/22 23:21)
- `gaze_scene_affine_0.5m.json` — 9점, 17.09px
- `gaze_scene_affine_1.0m.json` — 9점, 15.67px

**절차** (사용자가 검증한 방법은 `m` = 다점 affine):
1. 안경 착용 → 눈 창에서 동공에 노란 타원이 맞는지 확인
2. 정면 응시한 채 **`F`** — 눈알 구 모델 고정. 화면 우측 `mc=N`이 **30 이상**이어야 한다
   (초록이면 OK, 주황이면 부족)
3. **`m`** → 씬 창에서 타겟을 **보면서** 그 지점을 클릭. 반복
4. **`s`** 저장

⚠️ 미해결 문제 2개:
- **시선 벡터 재현성** — 같은 곳을 봤는데 `[0.054,0.66,0.749]` → `[0.54,0.432,0.722]`로
  달라진 적이 있다. 원인 미규명.
- **커버리지** — 최고 기록 5.9px는 화면 좁은 범위만 찍은 결과였다. 실제 쓸 거리
  (컵이 놓일 0.5~0.8m)에서, 컵이 놓일 범위 전체에 점을 흩어야 한다.

---

## 6. 미해결로 남기는 것

### (a) `fuse_and_send`의 포즈 게이트가 불충분하다 ★버그

`fuse_and_send()`는 "포즈가 0.3초 안 오면 추적 상실"로 게이트를 걸고 주석에도
그렇게 적었다. **틀렸다.** ORB-SLAM3는 발산 중에도 30Hz로 포즈를 계속 낸다.
실측에서 `pos=(+565.948, +174.696, -144.760)` (565미터)가 "신선한" 포즈로 통과했다.

고치려면 속도 기반 sanity 체크가 필요하다: 연속 포즈 간 이동이 프레임당
임계(예: 0.5m)를 넘으면 거부. 원점 근처로 점프하면 맵 리셋이므로 이전 세계좌표는
전부 무의미해진다.

**태그 직결로 가면 이 코드 경로 자체를 안 쓰게 된다.** 그래도 남겨두면 나중에
SLAM을 다시 켤 때 조용히 틀린다.

### (b) baseline 1.63배 충돌 — 미측정

```
GEOMETRIC_BASELINE_M = 0.1716 m   ← ORB-SLAM3도 이 값
DEPTH_BASELINE_M     = 0.105  m   ← SGBM이 멀게 나온다고 경험적으로 낮춤
```

융합이 이 둘을 섞어 쓴다(카메라 위치는 SLAM 스케일, 대상까지 거리는 SGBM 스케일).
컵이 60cm에 있는데 37cm로 계산되면 팔이 허공을 집는다.

측정 도구 2개를 만들었지만 **둘 다 결론을 못 냈다** — SLAM이 불안정해서:
- `verify_depth_scale.py` — 줄자 기준. 아직 안 돌려봄
- `verify_scale_vs_slam.py` — SLAM을 자로 씀. 돌렸으나 SLAM이 계속 발산해서
  배율이 0.035~907로 널뛰었다(무효)

**태그 직결 + 테이블 평면으로 가면 이 문제가 사라진다.** 태그 6cm가 절대 스케일을
주고, 거리는 평면 교차로 나온다. 스테레오 깊이를 안 쓰면 baseline이 무관해진다.

### (c) 태그 번들 위치가 "자로 잰 값"이다 — 가장 약한 고리

`arm/config.yaml`의 `anchor.bundle` 좌표는 전부 자리표시자(`MEASURE` 주석)다.
핸드아이 캘리브레이션이 없어서 사람이 자로 재서 타이핑해야 한다. 그 오차가
그대로 팔 좌표 오차가 된다.

`arm/README.md:128`이 더 나은 방법을 미완료로 적어놨다: 그리퍼에 태그 1장을 더
붙이면 FK와 짝지어 번들 좌표를 **자동 산출**할 수 있다.

---

## 7. 환경 메모 (반복해서 물리는 것들)

- **conda가 ROS2를 깨뜨린다.** 항상:
  ```bash
  export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
  unset PYTHONPATH CONDA_PREFIX CONDA_DEFAULT_ENV
  ```
  pip은 `/usr/bin/python3 -m pip install --user --break-system-packages`
- **`pkill -f` 패턴에 `[_]`를 넣을 것.** 안 그러면 그 명령을 실행 중인 셸 자신이
  매칭돼서 죽는다. 오늘 Pi 종료가 이것 때문에 실패했다.
- **파이썬 백그라운드는 `-u`로.** 아니면 stdout이 버퍼링돼서 종료 전까지 안 보인다.
- `pupil_apriltags` 1.0.4는 `~/.local`에 `--user` 설치 → venv에서 안 보인다.
- Pi: `a-hri@192.168.50.189`, 무암호 sudo 됨. 카메라 스택 `~/ocams.sh`.
- 데스크탑 sudo 암호는 사용자가 알려준 것이 있다(대화 기록 참조).
- SD카드 부팅이므로 Pi 전원 뽑기 전 반드시 `sudo systemctl poweroff`.

---

## 8. 커밋

- `1b59a39` — 융합 UDP 송신 + AprilTag 시트/검증 도구 (이 저장소)
- **미커밋**: `ros2_ws/src/orbslam3_ros2/` (별도 클론, detached HEAD).
  버퍼 수정이 작업 트리에만 있다. 그 디렉토리에서 `git reset --hard` 금지.
- **미커밋**: `ros2_ws/src/gaze_hri/` 하위 6개 파일 — 이전 세션 변경, 손대지 않았다.
- `~/slam.sh`는 저장소 밖(홈)에 있다.
