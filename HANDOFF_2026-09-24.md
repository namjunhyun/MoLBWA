# MoLBWA 인수인계 — 2026-09-23 저녁 → 다음 세션

시선으로 컵을 고르면 SO-101 팔이 집어 옮긴다. **태스크는 B(컵 옮기기, `gaze_hri` 스택)로 확정**,
컵 위치는 **탑다운 카메라**, 시선→로봇 좌표는 **AprilTag 직결**(SLAM 없음).
이전 문서: `HANDOFF_2026-09-23.md`(SLAM 포기 이유), 노트: `notes/2026-09-23_gaze_calib_rerun.md`.

---

## ⚠️ 먼저 읽을 것 — 하드웨어 상태

- **팔로워가 home 자세에서 토크 ON 으로 버티는 중일 수 있다** (사용자가 "그대로 두기" 선택).
  **그대로 전원을 뽑으면 팔이 떨어진다.** 끝내려면:
  ```bash
  cd ~/MoLBWA-gaze-hri
  python3 tools/so101_goto.py rest          # 저장된 휴식 자세로 (~7초)
  python3 tools/so101_jog.py relax --yes     # 토크 OFF — 그 뒤 전원 OK
  ```
- **포트**: `/dev/ttyACM0` = 리더(5V), `/dev/ttyACM1` = **팔로워(12V)**. 번호는 바뀔 수 있어
  맵 파일이 by-id 경로(`usb-1a86_..._5B8E115667`)를 쓴다. 드라이버는 9V 미만이면 연결 거부.
- Pi(안경 카메라)는 꺼져 있다. 켜면 `ssh a-hri@192.168.50.189 '~/ocams.sh'`.
  눈 카메라 USB 가 재열거되면 `~/eye_restart.sh` (Pi 홈).

## 오늘 한 것 (커밋 순)

| 커밋 | 내용 |
|---|---|
| ec732b0 | 뷰어 exit 1 회귀 2개 수정, **F 키(안구모델 고정) 연결 — 원래 한 번도 동작 안 했음**, affine 입력 (x,y) |
| b826ed9 | `origin/fix/so101-runnable` 의 `arm/` 가져오기 |
| c6265c0, 47c377b | 태그 직결 글루: `gaze_on_scene --send-gaze-px` → UDP 55056 → `arm/gaze_tag_bridge.py` → UDP 55055(점+머리) → `gaze_hri`. 런치 `gaze_frame:=base_link` |
| b578f74 | `gaze_hri` 광선 스냅 (테이블 교차점 점-점 비교는 컵보다 8.9cm 뒤 → 스냅 실패였다) |
| 6c4e301 ~ d919569 | **SO-101 팔로워 안전 드라이버** + 실측 관절 맵 + 실기 구동 |

## 로봇팔 — 어디까지 됐나

GitHub 의 `arm_server` feetech 백엔드는 **그대로는 실행 불가**였다 (SDK API 없음, 기본 포트가
리더, 틱↔각도 가정, home 이 책상 아래, 충돌해도 자동 복귀). `gaze_hri/so101_driver.py` 로 재작성:
EEPROM 쓰기 금지, 한계 밖 관절이면 토크 거부, 토크 50%, 추종오차/부하/온도/전압 감시 →
그 자리 hold, 통신 오류 값 방어(위치 이상 시 마지막 목표 유지). 테스트 `gaze_hri` 52/52.

실측 관절 맵 `ros2_ws/src/gaze_hri/config/so101_follower.yaml`:
- zero = [**1794**, 3084, 1094, 2095, 950, 2044], sign = [-1, -1, -1, -1, 1, 1]
- 5관절 조그로 방향 육안 확인 완료. pan 은 두 번 보정 (부호 반전, 0점 -14.2°)
- home 이동 후 실측: 좌우 ≈ 0, 손목축→고정 손가락 끝 **18cm 실측** (계산 17.4cm) → 맵 OK

**남은 것 (이 순서로)**
1. **`l3`(손목축→파지 지점) 확정** — 지금 설정 0.110 은 추정치고 손가락 끝(0.18)보다 7cm 짧다.
   시연 컵 지름과 그리퍼에 들어가는지 확인 후 `l3 ≈ 0.18 - 컵반지름 - 0.01`.
   ★ `l3` 는 `gaze_hri.yaml` 의 **여러 섹션**(arm_server, calibrate_world_to_base, control_panel,
   topdown_click)과 `ArmGeometry` 기본값에 흩어져 있다. `tools/so101_goto.py` 는 기본값을 쓴다 — 전부 맞출 것.
2. **파지 높이 여유 계산** — 45° 접근이면 손가락 끝이 파지점보다 ~3.5cm 낮고, 팔꿈치가 중력으로
   ~2.4° 처진다(home 에서 TCP 1.1cm 낮음). 책상 긁힘 위험. `grasp_height`/접근각 재검토
3. 태그 좌표 실측 → `arm/config.yaml` `anchor.bundle` (태그 56mm 반영됨). 측정법은 대화 기록/§아래
4. 탑다운 캘리브 (`ROBOT_ONLY_TEST.md` 3단계). ★ 그 문서의 서보 절차는 옛 백엔드 기준이다
5. 시선 캘리브(안경 쓸 때마다) → 전체 통합

## 이번 세션에 새로 물린 함정

- **`pkill -f`/`pgrep -f` 자기 매칭 — 오늘 두 번 더.** `[_]` 트릭도, 같은 명령 문자열에 원래
  이름이 그대로 있으면 소용없다(`nohup python3 gaze_on_scene.py` 를 같은 명령에 쓰는 경우,
  `pgrep -f "install/gaze_hri/lib"` 가 셸 자신과 매칭). **kill 과 launch 를 다른 명령으로.**
- **rclpy 는 SIGTERM 을 가로채 컨텍스트만 내린다** → 루프가 `rclpy.ok()` 를 안 보면 kill 로 안 죽는다
  (뷰어/브리지/run_demo 는 고침). 띄운 ROS 런치가 안 죽고 남아 다음 테스트를 오염시킨 적 있음.
- **scservo_sdk**: 읽기는 `(data, result, error)` 3개, 쓰기는 `(result, error)` **2개**.
- **서보 EEPROM(영점/범위)이 세션 중 한 번 바뀌어 있었다** (두 팔 모두, lerobot 재캘리브로 추정,
  원인 미확인). 맵은 바뀐 뒤 상태로 측정했다. 또 바뀌면 맵 재측정 필요 —
  `tools/so101_probe.py` 로 범위/homing 을 맵의 `captured` 시점과 비교할 것.
- 관절 맵 D 자세(회전)는 **"팔을 마주 본 자리에서 본인의 오른쪽"** 한 가지 기준만. 맵 만든 뒤
  반드시 `so101_jog.py 1 --deg 10` 으로 눈 확인.
- 시선 캘리브: `f`(구 고정) → `m` → 9~12점. 한/영 입력 상태면 키가 안 먹는다. 9점부터 자동저장인데
  불량점(중앙값 2.5배)이면 저장 안 함, `u` 로 마지막 점 취소.

## 도구 요약

| 도구 | 용도 |
|---|---|
| `tools/so101_probe.py [--watch]` | 읽기 전용 서보 점검 (리더/팔로워 구분) |
| `tools/so101_map_calib.py relax/capture/solve` | 틱↔각도 맵 실측 |
| `tools/so101_jog.py J --deg N` | 관절 하나 왕복 (방향 확인) |
| `tools/so101_goto.py save-rest/home/rest/xyz` | 경로 검사 후 이동 |

## git

- 브랜치 `feat/gaze-hri-pick-place`. **원격에 안 올린 로컬 커밋 18개**, **로컬에 없는 원격 커밋 4개**
  (팀원 superboowan: SGBM 깊이 스케일, 1m 매핑, WORKLOG). 푸시 전에 합쳐야 한다 —
  `src/gaze_on_scene.py`, `calibration/gaze_scene_affine*.json`, `src/ocams_calib.py` 가 겹친다.
- 미커밋: `control_panel_node.py`, `gui.launch.py` (이전 세션 것, 손대지 않음), `ros2_ws/src/orbslam3_ros2/`(별도 클론, **reset/clean 금지**).
